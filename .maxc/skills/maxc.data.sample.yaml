skill:
  id: maxc.data.sample
  name: 数据采样
  version: "0.1.0"
  description: 返回表的样例数据，适合探查数据内容。
  input:
    schema:
      table:
        type: string
        required: true
      rows:
        type: integer
        default: 5
  implementation:
    type: builtin
    target: data.sample
