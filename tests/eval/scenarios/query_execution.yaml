scenarios:
  - name: query_partitioned_table
    description: |
      User asks: "Show me the top 10 rows from the user_behavior table
      in the analytics project."
    context:
      auth_status: authenticated
      project: current_workspace
      tables:
        - name: user_behavior
          project: analytics
          partitioned: true
          partition_columns: ["ds"]
    expected:
      required_commands:
        - "meta latest-partition"
        - "--json"
      required_behaviors:
        - "discovers partition value before constructing the query"
        - "uses the explicit analytics project and the identifier shape returned by metadata"
        - "includes partition filter in WHERE clause"
        - "uses LIMIT"
      forbidden_patterns:
        - "FROM user_behavior WHERE"
      key_assertions:
        - "never queries a partitioned table without checking latest partition first"
        - "does not infer an environment; uses the explicitly supplied project"
    difficulty: medium
